{
  "name": "sih26095-nirikshan-ai",
  "version": "1.0.0",
  "private": true,
  "description": "DoSJE Centralized Monitoring & Surprise Inspection Mobile App",
  "workspaces": [
    "packages/*",
    "backend",
    "mobile"
  ],
  "scripts": {
    "build:types": "npm --prefix packages/shared-types run build",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "backend:seed": "npm --prefix backend run prisma:seed",
    "mobile:start": "npm --prefix mobile run start",
    "mobile:web": "npm --prefix mobile run web",
    "type-check": "npm --prefix packages/shared-types run type-check && npm --prefix backend run type-check && npm --prefix mobile run type-check"
  }
}
